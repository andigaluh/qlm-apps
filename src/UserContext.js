import { createContext } from "react";

export const UserContext = createContext(null);
export const PersonContext = createContext(null);
export const MachineContext = createContext(null);
export const SparepartsContext = createContext(null);
export const ToolsContext = createContext(null);
export const InboxContext = createContext(null);
